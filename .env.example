# Copy to .env before running `docker compose up` (docker compose loads .env from the project
# root automatically — no extra flag needed). Never commit the real .env.
MSSQL_SA_PASSWORD=ChangeMe_Local123!
